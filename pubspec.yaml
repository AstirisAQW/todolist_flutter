name: todolist_flutter
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  firebase_core: ^3.15.1    # Firebase services
  cloud_firestore: ^5.6.11  # Firebase services
  get_it: ^8.0.3            # Clean way to provide dependencies
  equatable: ^2.0.7         # Helps in comparing objects, crucial for BLoC to know when a state has actually changed.
  bloc: ^9.0.0              # BLoC for state management
  flutter_bloc: ^9.1.1      # Che core package for integrating BLoC into Flutter.

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0


flutter:
  uses-material-design: true




